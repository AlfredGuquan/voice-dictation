.env
*.o
*.swp
.DS_Store

# SPM build artifacts (but keep .build/*.md knowledge files) — applies at any depth
.build/
**/.build/
!.build/*.md
!**/.build/*.md

# QA screenshots — stay local, may contain desktop/environment context
screenshots/

# Speedtest fixture audio — regenerable via eval/speedtest/generate_test_audio.sh
eval/speedtest/*.wav
eval/speedtest/*.aiff
